<% cookies.signed[:session_id] && if Session.find_by(id: cookies.signed[:session_id]) %>
  <%= button_to "Log out",
        session_path,
        method: :delete,
        class: "btn btn-danger",
        form: { style: "display:inline;" } %>
<% end %>
